# 2026-03-15

## MWPanel – wykonane zmiany (durable)

- PM2: uruchomiono i ustabilizowano procesy `mwpanel-api` oraz `v5-web`; naprawiono m.in. problemy z `vite` i `better-sqlite3`.
- Dashboard: usunięto kafel „Monitoring rynku” z głównego dashboardu; dodano osobny kompaktowy widget „Nowe oferty z rynku” i ustawiono go zgodnie z późniejszymi wymaganiami układu.
- Pipeline transakcji: przebudowano układ na pionowy/responsywny, bez poziomego scrolla.
- Mapa ofert: naprawiono geokodowanie (usunięto twarde ograniczenie tylko do PL przy geokodzie), dodano przycisk „Odśwież geolokalizację”.
- Monitoring ofert: wdrożono filtr „tylko Polska” (odrzucanie zagranicznych ofert na backendzie + ukrywanie w API); istniejące aktywne zagraniczne oferty zostały zarchiwizowane.
- Dashboard analityczny: dodano moduł analityki rynku (okazje, alerty, heatmapa) + 3 widgety dashboardowe.
- Alerty rynku: przejście z wersji „on the fly” na trwałe alerty w DB (`market_alerts`) + oznaczanie jako przeczytane (`read`/`read-all`) + filtr „tylko nieprzeczytane” i licznik unread.
- Admin / Użytkownicy: dodano pełny CRUD z UI (lista, dodawanie, edycja roli/statusu, usuwanie), z zabezpieczeniami (brak usunięcia/dezaktywacji własnego konta).
- Użytkownicy testowi: usunięci, pozostawiono `admin@mwpanel.pl`.
- Strona „Publikacja ofert”: wyczyszczona z mocków; zastąpiona placeholderem „moduł w przygotowaniu”.
- Monitoring rynku (kafelki): dodano badge okazji, cena/m², mediana porównawcza, statusy oferty, akcje „Importuj / Obserwuj / Porównaj”, oraz historię zmiany ceny.
- Watchlist/Compare: dodano trwały zapis w DB (`external_listing_watchlist`, `external_listing_compare`) + endpointy API i integrację w UI.
- „Twój panel” w sidebarze: statyczne liczby zastąpione danymi dynamicznymi (aktywne oferty, klienci, zadania).
- Naprawa krytyczna logowania 500: usunięto restart-loop backendu spowodowany błędem FK w seedzie `listings` (seed oparty teraz o realnie istniejące rekordy).
- Branding/logo:
  - podmieniono branding na `panel-logo.png` / transparentny wariant,
  - usunięto blok informacyjny z loginu (tekst o koncie backendowym),
  - kilkukrotnie dopasowano rozmiary logo zgodnie z feedbackiem,
  - finalnie przywrócono mniejszy rozmiar logo obok MW Partner i ponownie wycięto białe tło (`panel-logo-transparent.png`).
- Login V2: wykonano redesign premium (nowy hero, glass auth card), a następnie podmieniono tło lewej sekcji na subtelne animowane gradient/glow (bez gridu), bez zmian logiki auth.

## Istotny stan końcowy

- Procesy PM2: `mwpanel-api` i `v5-web` online.
- Backend API health odpowiada poprawnie.
- Alerty rynku, watchlist i compare działają w trybie trwałym (DB), nie tylko lokalnie/on-the-fly.

## Update 17:33 UTC

- Na prośbę użytkownika ponownie powiększono logo MW Panel w headerze po zalogowaniu (obok MW Partner):
  - zmiana klasy z `h-10` na `h-20` (2x większe względem poprzedniego ustawienia).
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 17:40 UTC

- Logo/branding:
  - przywrócono mniejszy rozmiar logo MW Panel obok MW Partner,
  - następnie ponownie zwiększono je 2x na prośbę użytkownika,
  - poprawiono transparentność pliku `panel-logo-transparent.png` (usuwanie białego tła).
- Login UI:
  - usunięto sekcję informacyjną „Użyj danych konta...”,
  - zastąpiono napis „MW Panel CRM System” obrazkiem `panel-logo` na loginie i po zalogowaniu.
- Login background premium:
  - usunięto grid na lewej stronie,
  - dodano animowane tło gradient + subtelne glowy (16/18/20s, ease-in-out, infinite),
  - dopasowano glass-card panel logowania (`rgba(15,23,42,0.72)`, blur 14px, delikatny border/shadow).
- Monitoring rynku:
  - wdrożono paginację ofert (kafelki + lista),
  - domyślnie 15 rekordów/stronę + wybór 2/4/6/8/15,
  - nawigacja: poprzednia/następna + numery stron,
  - poprawne blokady przycisków na pierwszej/ostatniej stronie,
  - reset do strony 1 przy zmianie filtrów, widoku i liczby elementów na stronę.
- Stabilność backendu:
  - naprawiono restart-loop API (FK constraint przy seedowaniu listings),
  - seed listings oparty o istniejące rekordy (`properties`, `agents`),
  - `/api/health` wróciło do statusu 200.

## Update 17:47 UTC

- Login UI: na prośbę użytkownika przyciemniono prawą stronę logowania (sekcja formularza):
  - tło prawej kolumny zmienione na `#05070d`,
  - glass-card panel przyciemniony do `rgba(8,12,20,0.78)`.
- Cel: bardziej elegancki, ciemniejszy wygląd premium bez zmian logiki auth.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 17:49 UTC

- Login (mobile UX): przełącznik motywu został odchudzony i dostosowany mobile-first.
  - Desktop: pozostawiono pełny przełącznik 3-opcyjny (Jasny/Ciemny/System).
  - Mobile: wprowadzono kompaktowy przycisk z ikoną aktualnego trybu + dropdown z 3 opcjami.
  - Aktywny tryb jest wyraźnie oznaczony.
- Cel: lżejszy wizualnie header mobilny, lepsza ergonomia i zachowanie pełnej funkcjonalności motywów.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 17:51 UTC

- Konsola/production hardening:
  - dodano favicony aplikacji (`public/favicon.ico`, `public/favicon.png`) i linki w `index.html` (redukcja błędów 404 assetów),
  - dodano globalny `AppErrorBoundary` (`src/components/AppErrorBoundary.tsx`) i opakowano nim `<App />` w `src/main.tsx`,
  - dodano obsługę `unhandledrejection` w produkcji z filtrowaniem znanych błędów zewnętrznych (rozszerzenia przeglądarki) oraz logowaniem tylko istotnych błędów aplikacji.
- Cel zmian: czystsza konsola produkcyjna, większa odporność UI na błędy runtime, bez ingerencji w logikę auth i architekturę aplikacji.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 17:54 UTC

- Motyw UI:
  - usunięto przełącznik motywu z górnego paska aplikacji (topbar),
  - dodano ustawienie motywu (Jasny/Ciemny/Systemowy) w sekcji „Mój profil” (zakładka dane profilu),
  - rozszerzono `themeStore` o zapis i odczyt preferencji **per użytkownik** (`mwpanel-theme-user:<userId>`),
  - po zalogowaniu motyw jest automatycznie synchronizowany i stosowany dla aktualnego usera (`syncThemeForUser`).
- Zachowano obecną architekturę i system theme provider; zmiany są modułowe i kompatybilne z istniejącym flow.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 17:58 UTC

- Login visual polish (premium animations):
  - dodano watermark logo MWPanel w lewej sekcji hero (bardzo niska opacity, subtelny ruch),
  - wdrożono animated gradient mesh (`meshShift`) + delikatne pulsowanie światła (`glowPulse`),
  - zachowano spokojny charakter animacji (długie czasy, ease-in-out, infinite),
  - dodano mikroanimacje kart „Role systemowe” (`roleDrift`) + subtelny hover lift.
- Zmiany wyłącznie UI (bez naruszenia logiki auth/formularza/architektury).
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:01 UTC

- Login branding cleanup:
  - usunięto małe logo „MWPanel CRM System” z górnej części lewej kolumny,
  - pozostawiono logo wyłącznie jako centralny watermark.
- Watermark tuning:
  - powiększono watermark około 2x (`w-[92%]`, `max-w-[980px]`),
  - zwiększono delikatnie widoczność (`opacity 0.10 / dark 0.13`),
  - zachowano subtelną animację watermarku.
- Tło premium (animated gradient mesh + light glow) pozostawiono bez zmian funkcjonalnych.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:03 UTC

- Dashboard: zmieniono wyłącznie kolejność sekcji layoutu.
  - Widgety: „Okazje z rynku”, „Alerty rynku”, „Heatmapa cen” przeniesiono na sam dół dashboardu,
  - pod sekcje: „Ostatnie aktywności”, „Nadchodzące zadania”, „Szybkie akcje”.
- Zachowano istniejący wygląd, funkcjonalność widgetów, grid i responsywność (bez refaktoru logiki).
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:06 UTC

- Login mobile dark mode: poprawiono czytelność logo „MW Partner Nieruchomości”.
  - W mobilnym bloku brandingu dodano styl: `dark:brightness-0 dark:invert dark:opacity-90`.
  - Efekt: tekst/logo jest wyraźne na ciemnym tle; desktop i light mode pozostają bez zmian układu/funkcji.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:08 UTC

- Header/topbar po zalogowaniu: wykonano delikatne zwężenie paska bez utraty czytelności logo/napisów.
  - `py-2` -> `py-1.5`,
  - odstęp logo `gap-3` -> `gap-2.5`,
  - wysokość logo MW Panel obok MW Partner: `h-20` -> `h-16`.
- Cel: lżejszy wizualnie topbar przy zachowaniu pełnej czytelności brandingu.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:17 UTC

- Topbar/header po zalogowaniu został dodatkowo zwężony na prośbę użytkownika (kolejny krok):
  - `py-1.5` -> `py-1`,
  - odstęp logo `gap-2.5` -> `gap-2`,
  - logo MW Panel obok MW Partner: `h-16` -> `h-12`.
- Cel: jeszcze smuklejszy pasek przy zachowaniu czytelności i bez ucinania elementów.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 18:50 UTC

- Użytkownik poprosił o instrukcję deploymentu na produkcję: co dokładnie kopiować.
- Przekazano zalecany zestaw plików/katalogów do synchronizacji (`src`, `server`, `public`, `Logo`, `supabase`, pliki konfiguracyjne i lockfile, opcjonalnie `data/mwpanel.sqlite` dla zachowania danych).
- Wskazano wykluczenia (`node_modules`, `dist`, `memory`, `test-results`, `.openclaw`, `.git`) oraz przykładowe polecenie `rsync`.
- Dodano checklistę kroków po stronie serwera: `npm ci`, `npm rebuild better-sqlite3`, `npm run build`, `pm2 restart`, `pm2 save`, plus szybka weryfikacja (`pm2 list`, healthcheck API).

## Update 18:52 UTC

- Zweryfikowano bieżącą listę użytkowników w bazie (`users` + `profiles`).
- Aktualny stan kont:
  - `admin@mwpanel.pl` — `admin`, `active`
  - `pytlak.tomek@gmail.com` — `agent`, `active`
  - `szef@mwpanel.pl` — `manager`, `active`

## Update 18:55 UTC

- Uprawnienia panelu administracyjnego zostały zaostrzone zgodnie z wymaganiem:
  - menu „Admin” ukryte dla ról `agent` i `manager` (widoczne tylko dla `admin`),
  - route `/admin` na frontendzie dostępna tylko dla `admin`,
  - backendowe endpointy administracyjne użytkowników (`/api/admin/users` GET/POST/PATCH/DELETE) ograniczone do `role === 'admin'`.
- Cel: panel administracyjny wyłącznie dla administratora, bez możliwości obejścia przez URL/API.
- Build frontendu i restart usług PM2 wykonane; `mwpanel-api` i `v5-web` online.

## Update 18:59 UTC

- Header/topbar: kliknięcie logo „MW Partner Nieruchomości” (obok hamburgera) otwiera teraz `https://mwpartner.pl` w nowej karcie.
- Implementacja: anchor z `target="_blank"` i `rel="noopener noreferrer"`; bez zmian w pozostałym układzie topbara.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 22:24 UTC

- Header branding behavior:
  - kliknięcie logo „MW Partner Nieruchomości” w topbarze otwiera `https://mwpartner.pl` w nowej karcie (`target="_blank"`, `rel="noopener noreferrer"`).
- User profile avatar:
  - dodano obsługę zmiany avatara po kliknięciu ikonki aparatu w profilu,
  - input `type=file` (`accept=image/*`), walidacja typu i limitu rozmiaru (max 3MB),
  - podgląd avatara w kółku profilu po wyborze pliku,
  - avatar renderowany także w topbarze zamiast inicjałów,
  - avatar przechowywany w stanie profilu (`authStore`) i utrzymywany przez persist store.
- Build/restart:
  - frontend zbudowany i zrestartowany; `v5-web` online.

## Update 22:28 UTC

- Login mobile branding (dark/light): poprawiono kolory napisu/logo „MW Partner Nieruchomości” zgodnie z wymaganiem.
  - `MW` zawsze niebieskie,
  - `Partner`: light = czarne, dark = białe,
  - `Nieruchomości` zawsze niebieskie.
- Implementacja dotyczy wyłącznie mobilnego bloku brandingu na loginie; desktop bez zmian.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 22:45 UTC

- Login/header branding + layout adjustments:
  - usunięto logo „MW Panel” z topbara aplikacji (obok MW Partner),
  - logo „MW Panel” wykorzystano jako globalny watermark tła strony logowania,
  - watermark ustawiono centralnie, jako warstwę tła (`pointer-events: none`) z niską widocznością i subtelną animacją,
  - zachowano animowane tło gradient mesh + light glow,
  - na mobile przesunięto sekcję logowania wyżej (mniej pustej przestrzeni nad kontenerem),
  - dopracowano warstwy/z-index, aby formularz i header pozostały czytelne i klikalne.
- Stabilność: bez zmian logiki auth; frontend zbudowany i zrestartowany; `v5-web` online.

## Update 22:51 UTC

- Korekta mobile branding loginu (zgodnie z doprecyzowaniem):
  - przywrócono oryginalny wygląd logo MW Partner w widoku mobilnym,
  - dodano wariant SVG dla dark mode mobile: `Logo/mw-logo-mobile-dark.svg`,
  - logika renderu: mobile używa `mwLogo` w light oraz `mwLogoMobileDark` w dark,
  - efekt kolorystyczny: `MW` i `Nieruchomości` pozostają niebieskie, `Partner` jest biały w dark mode.
- Zmiana dotyczy wyłącznie mobile login branding; pozostała funkcjonalność i layout bez zmian.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:07 UTC

- Login watermark visibility tuning:
  - użytkownik zgłosił, że znak wodny MW Panel jest niewidoczny,
  - podniesiono warstwę watermarku (`z-[5]`), pozostawiając treść loginu na wyższej warstwie,
  - zwiększono rozmiar watermarku (`w-[135%]`, `max-w-[1500px]`),
  - ustawiono bardziej czytelną, ale nadal subtelną przezroczystość (`opacity 0.18` light / `0.14` dark),
  - zachowano subtelną animację watermarku.
- Cel: watermark widoczny, ale nieprzeszkadzający w czytelności formularza.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:13 UTC

- Login UI cleanup: usunięto napis „dostęp oparty o role” z sekcji formularza logowania.
- Zmiana wyłącznie wizualna (bez zmian logiki auth).
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:16 UTC

- Header po zalogowaniu: dopracowano wariant logo MW Partner dla mobile dark mode.
  - Dodano import `mw-logo-mobile-dark.svg` do `App.tsx`.
  - Mobile (`md:hidden`) renderuje:
    - dark: `mwLogoMobileDark` (jaśniejszy napis „Partner”),
    - light: oryginalne `mwLogo`.
  - Desktop (`hidden md:block`) pozostaje na oryginalnym `mwLogo` bez zmian.
- Cel: poprawa czytelności napisu „Partner” tylko w trybie mobilnym ciemnym, bez wpływu na desktop/light.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:17 UTC

- Dashboard „Nowe oferty z rynku”:
  - zwiększono liczbę wyświetlanych ofert z 5 do 6 (`slice(0, 6)`),
  - dostosowano siatkę desktopową z `xl:grid-cols-5` do `xl:grid-cols-6`, aby lepiej wypełnić obszar.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:24 UTC

- Użytkownik zapytał o rekomendowany model działania dzwonka powiadomień.
- Zaproponowano podejście produktowe:
  - dzwonek jako centrum rzeczy wymagających reakcji,
  - podział na priorytety (high/medium/low),
  - ograniczenie do istotnych zdarzeń (lead, zadanie przeterminowane, błąd importu, okazje),
  - UX: unread count, lista z CTA, mark read / mark all, filtry,
  - antyspam: deduplikacja i limity burst,
  - docelowo tabela `notifications` per user z powiązaniem `entityType/entityId/url`.

## Update 23:28 UTC

- Login/header brand consistency:
  - dopracowano zachowanie logo MW Partner po zalogowaniu dla mobile dark mode,
  - `App.tsx` korzysta z `mw-logo-mobile-dark.svg` wyłącznie dla mobile+dark,
  - mobile+light i desktop pozostają na oryginalnym logo.
- Cel: poprawa czytelności napisu „Partner” bez wpływu na pozostałe widoki/tryby.
- Frontend zbudowany i zrestartowany; `v5-web` online.

## Update 23:43 UTC

- Dodano komponent zegara aplikacyjnego `src/components/AppClock.tsx`:
  - aktualizacja co 1 sekundę (`setInterval` + cleanup),
  - format 24h `HH:mm:ss`, locale `pl-PL`, timezone `Europe/Warsaw`,
  - desktop: data + czas, mobile: tylko czas,
  - `tabular-nums` dla stabilnej szerokości cyfr.
- Zegar zintegrowano w topbarze aplikacji (`App.tsx`) po stronie prawej.
- Login page nie wyświetla zegara (zegar tylko w app shell po zalogowaniu).
- Build frontendu i restart PM2 wykonane; `v5-web` online.

## Update 23:50 UTC

- Dashboard premium UI refactor (UI/UX only, bez zmian logiki biznesowej):
  - przeprowadzono audit struktury dashboardu i widgetów marketowych,
  - zidentyfikowano obszary logic-sensitive i pozostawiono je nietknięte (API contracts, routing, permissions, data flow),
  - odświeżono warstwę prezentacji:
    - bardziej kompaktowy top overview,
    - mniejsze/czystsze karty statystyk,
    - poprawione spacingi i hierarchię sekcji aktywności/zadań,
    - cleaner quick actions (spójniejszy dark premium styl),
    - premium look dla kart marketowych,
    - lepsze loading states (skeleton) i empty states,
    - poprawiona czytelność danych heatmapy.
- Zakres zmian: `Dashboard.tsx` + widgety w `src/components/dashboard/*` (styling/layout only).
- Build frontendu i restart PM2 wykonane; `v5-web` online.
- Ryzykowne obszary świadomie niezmieniane: mechanika fetchów, akcje biznesowe, kontrakty danych.
