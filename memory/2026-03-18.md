# Memory 2026-03-18

## 2026-03-18 00:04 UTC — durable summary (CRM dashboard data consistency + header/UI refactor in progress)

- Rozpoczęto wdrażanie wymagań usera dot. spójności danych dashboardu nieruchomości (backend jako source of truth):
  - backend: dodano canonical normalizację statusów ofert `active|inactive|sold|expired` przez `normalizeListingStatus(...)`,
  - backend: `mapListing` zwraca znormalizowany status,
  - backend: zapisy `POST/PATCH /api/listings` normalizują status przed persist,
  - backend: dodano endpoint `GET /api/dashboard/listing-stats` (auth), który zwraca gotowe statystyki ofert z DB:
    - `totalOffers`, `activeOffers`, `expiringOffers`,
    - dodatkowo: `inactiveOffers`, `soldOffers`, `expiredOffers`, `portfolioValue`, `avgPricePerM2`, `statuses`.
  - endpoint obsługuje zarówno ścieżkę PostgreSQL, jak i SQLite fallback.
- Frontend `Dashboard.tsx` przepięty z lokalnego liczenia na dane backendowe:
  - dodano stan `listingStats` + fetch `/dashboard/listing-stats`,
  - usunięto lokalne liczenie aktywnych/wygasających ofert z tablicy `listings`,
  - KPI „wartość portfela” i „średnia cena/m²” korzystają z danych backendowych,
  - fallbacki bez placeholdera `—` (używane wartości liczbowe `0` / tekst `Brak danych`).
- Frontend `App.tsx`:
  - top-right przebudowany pod avatar menu (`UserMenu`) z dropdownem: Profil / Wyloguj,
  - logo w headerze poprawione pod brak przycinania (`object-contain`, większe wysokości, lepsze wyrównanie),
  - usunięto border pod headerem (likwidacja „pustego paska”),
  - zegar w `AppClock` uproszczony do zwykłego czasu systemowego `HH:mm:ss` (bez countdown/badge look),
  - sidebar „Aktywne oferty” przestawiono na wartość z backendowego `/dashboard/listing-stats` (bez lokalnego `filter`).
- Typy:
  - dodano `OfferStatus` (canonical enum) w `src/types/index.ts` bez psucia legacy `ListingStatus`.
- Stan techniczny na koniec wpisu:
  - `node --check server/index.js` OK,
  - build frontu OK,
  - ostatni batch zmian jest przygotowany, ale po tym konkretnym batchu restart PM2 nie został jeszcze wykonany (przerwane przez memory flush).

## 2026-03-18 00:06 UTC — durable update (post-deploy verification)

- Wykonano restart po wdrożeniu zmian:
  - `pm2 restart mwpanel-api --update-env`
  - `pm2 restart v5-web --update-env`
  - oba procesy online.
- Zweryfikowano runtime:
  - `GET /api/health` zwraca `ok: true`.
- Zweryfikowano nowy backendowy endpoint statystyk ofert:
  - `GET /api/dashboard/listing-stats?agencyId=agency-1` (z auth) zwraca poprawny payload,
  - canonical statuses zwracane jako `['active','inactive','sold','expired']`.
- Zweryfikowano normalizację statusów ofert na liście:
  - `GET /api/listings?agencyId=agency-1` zwraca znormalizowane statusy (w testowych danych: `inactive`).
- Drobna kontrola UI fallbacków:
  - w dashboardzie nie znaleziono placeholdera `—` dla braków danych (poza znakiem użytym jako separator tekstowy, nie jako placeholder danych).

## 2026-03-18 00:13 UTC — durable update (UI polish continuation)

- Kontynuowano przejście UI na spójny styl cyberpunk/toxic w kolejnych modułach:
  - `Properties.tsx`:
    - CTA przełączone na `btn-primary`,
    - akcje „Szczegóły/Edytuj” na kartach dopasowane do accent + dark hover,
    - akcent ceny dopasowany do `--accent-main`.
  - `TasksList.tsx`:
    - kluczowe przyciski akcji i zapisu przełączone na `btn-primary`.
  - `Reports.tsx`:
    - główny przycisk akcji przełączony na `btn-primary`.
- Operacyjnie po zmianach:
  - build frontu przeszedł poprawnie,
  - restart `v5-web` przez PM2 wykonany,
  - `mwpanel-api` pozostał online i stabilny.

## 2026-03-18 00:22 UTC — durable update (mobile header/layout finalization)

- Na wyraźne żądanie usera domknięto poprawki mobile header/dashboard:
  - zegar usunięty całkowicie z topbara (`AppClock` usunięty z layoutu, bez pustego kontenera/rezerwacji miejsca),
  - usunięto „pusty pasek” pod headerem przez korektę układu i spacingów (`header`/`main`/top sekcja dashboardu),
  - poprawiono ekspozycję logo na mobile (bez przycinania):
    - kontener logo `overflow-visible`,
    - dopracowane `max-width`, `object-contain`, alignment i prawy padding.
- Zmiany wykonane głównie w:
  - `src/App.tsx` (header + parent layout),
  - `src/components/Dashboard.tsx` (górna sekcja mobile).
- Operacyjnie:
  - build frontu OK,
  - restart `v5-web` przez PM2 OK,
  - runtime pozostał stabilny.

## 2026-03-18 00:33 UTC — durable update (final mobile micro-polish)

- Dociągnięto mikro-poprawki mobilne po feedbacku usera:
  - logo mobile: podniesiono elastyczny limit szerokości do `max-w-[calc(100vw-88px)]`,
  - layout główny mobile: usunięto dodatkowy top padding (`main` z `pt-0` na mobile),
  - efekt: dashboard startuje wyżej, bez martwej strefy pod headerem.
- Operacyjnie:
  - build frontu OK,
  - restart `v5-web` przez PM2 OK,
  - `mwpanel-api` nadal online.

## 2026-03-18 00:35 UTC — durable update (header strip + mobile user icon)

- Po kolejnym feedbacku usera wykonano finalny fix topbara:
  - usunięto efekt „paska pod logo” przez zmianę headera z `fixed` na `sticky` i usunięcie offsetu wrappera (`pt-14`),
  - włączono widoczność ikony użytkownika na mobile (UserMenu nie jest już ukryty pod `md`),
  - dropdown użytkownika (Profil/Wyloguj) pozostaje aktywny i responsywny.
- Operacyjnie:
  - build frontu OK,
  - restart `v5-web` przez PM2 OK,
  - runtime stabilny.

## 2026-03-18 00:37 UTC — durable update (new mobile menu button placement)

- Zmieniono trigger menu mobilnego zgodnie z prośbą usera:
  - usunięto stary pływający FAB menu z dołu ekranu,
  - dodano nowy przycisk `MENU` w headerze (lewa strona, przy logo), tylko na mobile,
  - styl przycisku dopasowano do obecnego cyber/dark UI (border + toxic accent).
- Operacyjnie:
  - build frontu OK,
  - restart `v5-web` przez PM2 OK,
  - runtime stabilny.

## 2026-03-18 00:43 UTC — durable update (mobile menu icon only)

- Na kolejną prośbę usera uproszczono mobilny trigger menu:
  - usunięto etykietę `MENU`,
  - pozostawiono samą ikonkę hamburgera w tym samym miejscu headera.
- Operacyjnie:
  - build frontu OK,
  - restart `v5-web` przez PM2 OK,
  - `mwpanel-api` nadal online.
