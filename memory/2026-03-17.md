
## 2026-03-17 01:59 UTC — durable summary (pre-compaction)
- Domknięto kolejne pakiety refaktoru i „dead actions”:
  - Leads:
    - inline status update z listy (`Leads.tsx`),
    - nowy widok szczegółów `LeadDetail.tsx` pod trasą `/leads/:id`,
    - zapis status/source/propertyInterest/followUpDate/notes.
  - Documents:
    - backend fallback download `GET /api/documents/:id/download` (redirect do URL, lokalny plik albo generacja PDF fallback z `content` przez Playwright),
    - front przepięty na ten endpoint (Documents + ClientDetail).
  - Ujednolicenie helperów downloadu dokumentów:
    - `src/utils/authToken.ts`,
    - `src/utils/documentDownload.ts`,
    - wspólne `buildDocumentDownloadUrl(doc.id)` użyte w aktywnych miejscach.
- Login reset hasła wdrożony end-to-end:
  - API: `POST /api/auth/forgot-password`, `POST /api/auth/reset-password`,
  - DB: `password_reset_tokens`,
  - UI: modal „Zapomniałeś hasła?” w `Login.tsx`.
- Admin / BusinessSuite / UserProfile:
  - wszystkie wykryte pozorne akcje bez backendu są twardo wyłączone (`disabled`) i opisane tooltipem,
  - końcowy sweep wykazał wyłącznie `disabled_no_click` (brak aktywnych martwych klików).
- Stabilizacja/deploy:
  - po zmianach wykonywano build i restarty PM2 (`mwpanel-api`, `v5-web`) — procesy online.

## 2026-03-17 02:08 UTC — durable update (pre-compaction)
- Wdrożono pakiet personalizacji i role-based UI:
  - Nowa warstwa preferencji widoków: `src/utils/viewPreferences.ts` (scoped per rola + klucz widoku, zapisywane w localStorage).
  - Dashboard (`Dashboard.tsx`):
    - przycisk „Dostosuj widok”,
    - włączanie/wyłączanie sekcji (KPI, stats, market offers, market intel, activities, tasks, quick actions, market widgets),
    - tryb gęstości: `compact` / `comfortable`,
    - preferencje zapisywane per rola,
    - domyślne zestawy sekcji różne dla `agent` / `manager` / `admin`,
    - akcja „Zarządzaj agentami” w quick actions widoczna tylko dla `manager` i `admin`.
  - Clients (`Clients.tsx`):
    - konfiguracja widoczności kolumn przez menu „Kolumny”,
    - persystencja preferencji kolumn per rola,
    - role-based domyślne zestawy kolumn.
  - Documents (`Documents.tsx`):
    - konfiguracja widoczności kolumn przez menu „Kolumny”,
    - persystencja preferencji kolumn per rola,
    - role-based domyślne zestawy kolumn.
- Po wdrożeniu: build frontu OK i restart PM2 (`v5-web`, `mwpanel-api`) wykonany; procesy online.

## 2026-03-17 02:19 UTC — durable update (pre-compaction)
- Wdrożono AI Assist v1 w architekturze MWPanel (agency-scoped):
  - DB:
    - `ai_runs` (audit wywołań AI),
    - `ai_entity_insights` (bieżące insighty per encja),
    - `ai_feedback` (feedback użytkownika),
    - rozszerzenie `transactions`: `ai_win_probability`, `ai_score_updated_at`, `ai_score_reason`,
    - indeksy pod wydajność.
  - API:
    - `POST /api/ai/summary` (client/transaction + `promptVersion`),
    - `POST /api/ai/next-best-action`,
    - `POST /api/ai/score-transaction`,
    - `GET /api/ai/insights`,
    - `POST /api/ai/feedback`,
    - `POST /api/ai/tasks/from-action`.
  - Logika AI v1:
    - hybryda rule-engine + explainability,
    - scoring liczony backendowo, nie przez LLM,
    - snapshot źródeł i zapis runów, wersjonowanie promptów.
  - Front:
    - `ClientDetail` dostał sekcję AI Insight:
      - generowanie podsumowania,
      - podgląd summary/keyPoints/openIssues,
      - feedback 👍/👎 do runu.
- Po wdrożeniu: build frontendu OK, restart PM2 (`mwpanel-api`, `v5-web`) i health API OK.

## 2026-03-17 23:25 UTC — durable summary (PostgreSQL cutover + UI style pass)
- Email/Resend moduł został domknięty produkcyjnie i zintegrowany z backendem projektu:
  - kolejka email (`email_templates`, `email_messages`, `email_attachments`),
  - endpointy `/api/emails/enqueue` i `/api/emails/process`,
  - status flow `queued -> sending -> sent/failed`, retry attempts i logowanie błędów,
  - sekrety tylko w env (`RESEND_API_KEY`, `EMAIL_FROM`, opcjonalny token procesu kolejki).
- Zrealizowano migrację architektury DB w kierunku "all-in PostgreSQL":
  - dodano warstwę `pg` i `pgCore`,
  - przygotowano schema init dla core + email,
  - dodano migrator `npm run pg:migrate:core` (SQLite -> PostgreSQL),
  - przepięto wiele kluczowych endpointów na dual-mode SQLite/PG (auth, clients, properties, listings, tasks, transactions/checklist, documents + versions, document usage, emails).
- Cutover wykonany na lokalny PostgreSQL:
  - ustawiono `DB_DRIVER=postgres` i działający `DATABASE_URL` do lokalnego serwera,
  - migracja danych zakończona sukcesem,
  - PM2 restart (`mwpanel-api`, `v5-web`) + health API OK,
  - smoke testy endpointów po auth przeszły (w tym dokumenty + wersjonowanie).
- SQLite zdegradowane do roli archiwalnej:
  - utworzono backup cutover w `data/archive/` (DB + wal + shm),
  - ustawiono pliki SQLite i backupy jako read-only,
  - potwierdzono integralność (hash source == backup).
- Frontend/UX:
  - usunięto blokadę hosta dla domeny produkcyjnej (`mwpanelpro.pl`, `www.mwpanelpro.pl` w allowlist Vite),
  - wdrożono pierwszy pass nowego stylu "Cyberpunk Industrial + Toxic Accent" (globalne tokeny, sidebar/nav active, card/button patterns, shell theme) bez pastelowego SaaS looku.

## 2026-03-17 23:33 UTC — durable update (post-cutover notes)
- Wykonano kolejny pass stylu "Cyberpunk Industrial + Toxic Accent":
  - globalny anti-SaaS remap klas `blue-*` w `src/index.css` na toxic accent,
  - Dashboard dostał `stat card` z pionowym akcentem i subtelnym hover `scale(1.01)`,
  - wykres trendu na Dashboardze przepięty z `#3b82f6` na `var(--accent-main)`.
- Istotna uwaga operacyjna po cutover:
  - po ustawieniu SQLite jako read-only API zaczęło crashować (`SQLITE_READONLY` w `server/db.js` podczas bootstrapa),
  - tymczasowo przywrócono prawa zapisu do `data/mwpanel.sqlite*`, aby utrzymać stabilny runtime,
  - wniosek: pełne "archival-only" dla SQLite wymaga najpierw usunięcia/wyłączenia wszystkich zapisów bootstrapowych SQLite podczas startu API.

## 2026-03-17 23:37 UTC — durable update (mobile header UX)
- Na prośbę usera dopracowano mobile topbar:
  - usunięto z mobile headera dzwoneczek (powiadomienia),
  - usunięto z mobile headera profil/wylogowanie,
  - zegar ustawiono centralnie i precyzyjnie na mobile (desktop zachowuje osobny układ).
- Zmiany wdrożone w:
  - `src/App.tsx` (visibility + pozycjonowanie),
  - `src/components/AppClock.tsx` (styling pod cyberpunk + responsywne detale).
- Po wdrożeniu: build OK, restart `v5-web` przez PM2, usługa online.

## 2026-03-17 23:42 UTC — durable update (cyberpunk UI continuation)
- Kontynuowano migrację wyglądu do "Cyberpunk Industrial + Toxic Accent":
  - Dashboard: CTA/przyciski i część akcentów przestawione z legacy blue na tokeny `--accent-main` / `--accent-hover`,
  - stat cards i quick actions dopasowane do nowej palety (mniej SaaS, bardziej industrial),
  - domknięto pozostałe niebieskie akcenty w headerze/notyfikacjach `App.tsx`.
- Operacyjnie:
  - build frontu zakończony powodzeniem,
  - restart PM2 `v5-web` wykonany,
  - `mwpanel-api` potwierdzony online po zmianach.

## 2026-03-17 23:50 UTC — durable update (cyberpunk UI sweep: clients+documents)
- Kolejny batch stylu "Cyberpunk Industrial + Toxic Accent" wykonany:
  - `Clients.tsx`:
    - CTA i submity przepięte na `btn-primary`,
    - awatary/ikony akcji przestawione na toxic accent + ciemne hover stany,
    - mniej rounded/friendly SaaS.
  - `Documents.tsx`:
    - główne akcje (`Generuj`, `Generator`) przepięte na `btn-primary`,
    - quick filtry (`all/recent/favorites`) przepięte na tokeny (`--accent-main`, `--bg-panel`, `--border-subtle`),
    - mobile FAB dopasowany do nowego industrialnego stylu.
  - `App.tsx`:
    - domknięte pozostałe niebieskie elementy notyfikacji na `--accent-main`.
- Operacyjnie:
  - build frontu OK,
  - restart PM2 `v5-web` OK,
  - `mwpanel-api` pozostaje online.
